// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { CallAccessSystem } from "../../systems/call-access-system/CallAccessSystem.sol";
import { ResourceId } from "@latticexyz/world/src/WorldResourceId.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type CallAccessSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "evefrontier", name: "CallAccessSystem" }))
CallAccessSystemType constant callAccessSystem = CallAccessSystemType.wrap(
  0x737965766566726f6e7469657200000043616c6c41636365737353797374656d
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title CallAccessSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library CallAccessSystemLib {
  error CallAccessSystemLib_CallingFromRootSystem();

  function addCallAccess(CallAccessSystemType self, ResourceId systemId, bytes4 functionId, address caller) internal {
    return CallWrapper(self.toResourceId(), address(0)).addCallAccess(systemId, functionId, caller);
  }

  function removeCallAccess(
    CallAccessSystemType self,
    ResourceId systemId,
    bytes4 functionId,
    address caller
  ) internal {
    return CallWrapper(self.toResourceId(), address(0)).removeCallAccess(systemId, functionId, caller);
  }

  function addCallAccess(CallWrapper memory self, ResourceId systemId, bytes4 functionId, address caller) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CallAccessSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _addCallAccess_ResourceId_bytes4_address.addCallAccess,
      (systemId, functionId, caller)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function removeCallAccess(CallWrapper memory self, ResourceId systemId, bytes4 functionId, address caller) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert CallAccessSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _removeCallAccess_ResourceId_bytes4_address.removeCallAccess,
      (systemId, functionId, caller)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function addCallAccess(RootCallWrapper memory self, ResourceId systemId, bytes4 functionId, address caller) internal {
    bytes memory systemCall = abi.encodeCall(
      _addCallAccess_ResourceId_bytes4_address.addCallAccess,
      (systemId, functionId, caller)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function removeCallAccess(
    RootCallWrapper memory self,
    ResourceId systemId,
    bytes4 functionId,
    address caller
  ) internal {
    bytes memory systemCall = abi.encodeCall(
      _removeCallAccess_ResourceId_bytes4_address.removeCallAccess,
      (systemId, functionId, caller)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(CallAccessSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(CallAccessSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), msg.sender);
  }

  function callAsRootFrom(CallAccessSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(CallAccessSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(CallAccessSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (CallAccessSystemType) {
    return CallAccessSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(CallAccessSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _addCallAccess_ResourceId_bytes4_address {
  function addCallAccess(ResourceId systemId, bytes4 functionId, address caller) external;
}

interface _removeCallAccess_ResourceId_bytes4_address {
  function removeCallAccess(ResourceId systemId, bytes4 functionId, address caller) external;
}

using CallAccessSystemLib for CallAccessSystemType global;
using CallAccessSystemLib for CallWrapper global;
using CallAccessSystemLib for RootCallWrapper global;
