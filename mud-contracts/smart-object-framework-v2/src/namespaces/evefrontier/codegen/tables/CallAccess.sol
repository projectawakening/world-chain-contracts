// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

// Import user types
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

library CallAccess {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "evefrontier", name: "CallAccess", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x746265766566726f6e7469657200000043616c6c416363657373000000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0001010001000000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32, bytes4, address)
  Schema constant _keySchema = Schema.wrap(0x003803005f436100000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (bool)
  Schema constant _valueSchema = Schema.wrap(0x0001010060000000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](3);
    keyNames[0] = "systemId";
    keyNames[1] = "functionId";
    keyNames[2] = "caller";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](1);
    fieldNames[0] = "hasAccess";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get hasAccess.
   */
  function getHasAccess(ResourceId systemId, bytes4 functionId, address caller) internal view returns (bool hasAccess) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Get hasAccess.
   */
  function _getHasAccess(
    ResourceId systemId,
    bytes4 functionId,
    address caller
  ) internal view returns (bool hasAccess) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Get hasAccess.
   */
  function get(ResourceId systemId, bytes4 functionId, address caller) internal view returns (bool hasAccess) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Get hasAccess.
   */
  function _get(ResourceId systemId, bytes4 functionId, address caller) internal view returns (bool hasAccess) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (_toBool(uint8(bytes1(_blob))));
  }

  /**
   * @notice Set hasAccess.
   */
  function setHasAccess(ResourceId systemId, bytes4 functionId, address caller, bool hasAccess) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((hasAccess)), _fieldLayout);
  }

  /**
   * @notice Set hasAccess.
   */
  function _setHasAccess(ResourceId systemId, bytes4 functionId, address caller, bool hasAccess) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((hasAccess)), _fieldLayout);
  }

  /**
   * @notice Set hasAccess.
   */
  function set(ResourceId systemId, bytes4 functionId, address caller, bool hasAccess) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((hasAccess)), _fieldLayout);
  }

  /**
   * @notice Set hasAccess.
   */
  function _set(ResourceId systemId, bytes4 functionId, address caller, bool hasAccess) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((hasAccess)), _fieldLayout);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(ResourceId systemId, bytes4 functionId, address caller) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(ResourceId systemId, bytes4 functionId, address caller) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(bool hasAccess) internal pure returns (bytes memory) {
    return abi.encodePacked(hasAccess);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(bool hasAccess) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(hasAccess);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(
    ResourceId systemId,
    bytes4 functionId,
    address caller
  ) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = ResourceId.unwrap(systemId);
    _keyTuple[1] = bytes32(functionId);
    _keyTuple[2] = bytes32(uint256(uint160(caller)));

    return _keyTuple;
  }
}

/**
 * @notice Cast a value to a bool.
 * @dev Boolean values are encoded as uint8 (1 = true, 0 = false), but Solidity doesn't allow casting between uint8 and bool.
 * @param value The uint8 value to convert.
 * @return result The boolean value.
 */
function _toBool(uint8 value) pure returns (bool result) {
  assembly {
    result := value
  }
}
