// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { AnchorSystem } from "../../systems/anchor/AnchorSystem.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type AnchorSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "evefrontier", name: "AnchorSystem" }))
AnchorSystemType constant anchorSystem = AnchorSystemType.wrap(
  0x737965766566726f6e74696572000000416e63686f7253797374656d00000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title AnchorSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library AnchorSystemLib {
  error AnchorSystemLib_CallingFromRootSystem();
  error AnchorSystem_AnchorNotFound(uint256 anchorId);
  error AnchorSystem_AnchorAlreadyExists(uint256 anchorId);
  error AnchorSystem_DeployableNotFound(uint256 deployableId);
  error AnchorSystem_DeployableNotAnchored(uint256 deployableId, uint256 anchorId);

  function createAnchor(AnchorSystemType self, uint256 smartObjectId) internal {
    return CallWrapper(self.toResourceId(), address(0)).createAnchor(smartObjectId);
  }

  function anchorDeployable(AnchorSystemType self, uint256 anchorId, uint256 deployableId) internal {
    return CallWrapper(self.toResourceId(), address(0)).anchorDeployable(anchorId, deployableId);
  }

  function unanchorDeployable(AnchorSystemType self, uint256 anchorId, uint256 deployableId) internal {
    return CallWrapper(self.toResourceId(), address(0)).unanchorDeployable(anchorId, deployableId);
  }

  function createAnchor(CallWrapper memory self, uint256 smartObjectId) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert AnchorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_createAnchor_uint256.createAnchor, (smartObjectId));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function anchorDeployable(CallWrapper memory self, uint256 anchorId, uint256 deployableId) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert AnchorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _anchorDeployable_uint256_uint256.anchorDeployable,
      (anchorId, deployableId)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function unanchorDeployable(CallWrapper memory self, uint256 anchorId, uint256 deployableId) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert AnchorSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(
      _unanchorDeployable_uint256_uint256.unanchorDeployable,
      (anchorId, deployableId)
    );
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function createAnchor(RootCallWrapper memory self, uint256 smartObjectId) internal {
    bytes memory systemCall = abi.encodeCall(_createAnchor_uint256.createAnchor, (smartObjectId));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function anchorDeployable(RootCallWrapper memory self, uint256 anchorId, uint256 deployableId) internal {
    bytes memory systemCall = abi.encodeCall(
      _anchorDeployable_uint256_uint256.anchorDeployable,
      (anchorId, deployableId)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function unanchorDeployable(RootCallWrapper memory self, uint256 anchorId, uint256 deployableId) internal {
    bytes memory systemCall = abi.encodeCall(
      _unanchorDeployable_uint256_uint256.unanchorDeployable,
      (anchorId, deployableId)
    );
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(AnchorSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(AnchorSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), msg.sender);
  }

  function callAsRootFrom(AnchorSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(AnchorSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(AnchorSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (AnchorSystemType) {
    return AnchorSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(AnchorSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _createAnchor_uint256 {
  function createAnchor(uint256 smartObjectId) external;
}

interface _anchorDeployable_uint256_uint256 {
  function anchorDeployable(uint256 anchorId, uint256 deployableId) external;
}

interface _unanchorDeployable_uint256_uint256 {
  function unanchorDeployable(uint256 anchorId, uint256 deployableId) external;
}

using AnchorSystemLib for AnchorSystemType global;
using AnchorSystemLib for CallWrapper global;
using AnchorSystemLib for RootCallWrapper global;
