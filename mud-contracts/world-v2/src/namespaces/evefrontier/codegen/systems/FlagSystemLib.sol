// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { FlagSystem } from "../../systems/flag/FlagSystem.sol";
import { CreateAndAnchorParams } from "../../systems/deployable/types.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type FlagSystemType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "evefrontier", name: "FlagSystem" }))
FlagSystemType constant flagSystem = FlagSystemType.wrap(
  0x737965766566726f6e74696572000000466c616753797374656d000000000000
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title FlagSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library FlagSystemLib {
  error FlagSystemLib_CallingFromRootSystem();

  function createFlag(FlagSystemType self, CreateAndAnchorParams memory params) internal {
    return CallWrapper(self.toResourceId(), address(0)).createFlag(params);
  }

  function getFlagClassId(FlagSystemType self) internal view returns (uint256) {
    return CallWrapper(self.toResourceId(), address(0)).getFlagClassId();
  }

  function createFlag(CallWrapper memory self, CreateAndAnchorParams memory params) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert FlagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_createFlag_CreateAndAnchorParams.createFlag, (params));
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function getFlagClassId(CallWrapper memory self) internal view returns (uint256) {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert FlagSystemLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_getFlagClassId.getFlagClassId, ());
    bytes memory worldCall = self.from == address(0)
      ? abi.encodeCall(IWorldCall.call, (self.systemId, systemCall))
      : abi.encodeCall(IWorldCall.callFrom, (self.from, self.systemId, systemCall));
    (bool success, bytes memory returnData) = address(_world()).staticcall(worldCall);
    if (!success) revertWithBytes(returnData);

    bytes memory result = abi.decode(returnData, (bytes));
    return abi.decode(result, (uint256));
  }

  function createFlag(RootCallWrapper memory self, CreateAndAnchorParams memory params) internal {
    bytes memory systemCall = abi.encodeCall(_createFlag_CreateAndAnchorParams.createFlag, (params));
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function getFlagClassId(RootCallWrapper memory self) internal view returns (uint256) {
    bytes memory systemCall = abi.encodeCall(_getFlagClassId.getFlagClassId, ());

    bytes memory result = SystemCall.staticcallOrRevert(self.from, self.systemId, systemCall);
    return abi.decode(result, (uint256));
  }

  function callFrom(FlagSystemType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(FlagSystemType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), msg.sender);
  }

  function callAsRootFrom(FlagSystemType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(FlagSystemType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(FlagSystemType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (FlagSystemType) {
    return FlagSystemType.wrap(resourceId.unwrap());
  }

  function getAddress(FlagSystemType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _createFlag_CreateAndAnchorParams {
  function createFlag(CreateAndAnchorParams memory params) external;
}

interface _getFlagClassId {
  function getFlagClassId() external;
}

using FlagSystemLib for FlagSystemType global;
using FlagSystemLib for CallWrapper global;
using FlagSystemLib for RootCallWrapper global;
