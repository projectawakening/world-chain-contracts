// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import schema type
import { SchemaType } from "@latticexyz/schema-type/src/solidity/SchemaType.sol";

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout, FieldLayoutLib } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema, SchemaLib } from "@latticexyz/store/src/Schema.sol";
import { PackedCounter, PackedCounterLib } from "@latticexyz/store/src/PackedCounter.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

FieldLayout constant _fieldLayout = FieldLayout.wrap(
  0x00a0050020202020200000000000000000000000000000000000000000000000
);

struct DeployableFuelBalanceData {
  uint256 fuelUnitVolume;
  uint256 fuelConsumptionPerMinute;
  uint256 fuelMaxCapacity;
  uint256 fuelAmount;
  uint256 lastUpdatedAt;
}

library DeployableFuelBalance {
  /**
   * @notice Get the table values' field layout.
   * @return _fieldLayout The field layout for the table.
   */
  function getFieldLayout() internal pure returns (FieldLayout) {
    return _fieldLayout;
  }

  /**
   * @notice Get the table's key schema.
   * @return _keySchema The key schema for the table.
   */
  function getKeySchema() internal pure returns (Schema) {
    SchemaType[] memory _keySchema = new SchemaType[](1);
    _keySchema[0] = SchemaType.UINT256;

    return SchemaLib.encode(_keySchema);
  }

  /**
   * @notice Get the table's value schema.
   * @return _valueSchema The value schema for the table.
   */
  function getValueSchema() internal pure returns (Schema) {
    SchemaType[] memory _valueSchema = new SchemaType[](5);
    _valueSchema[0] = SchemaType.UINT256;
    _valueSchema[1] = SchemaType.UINT256;
    _valueSchema[2] = SchemaType.UINT256;
    _valueSchema[3] = SchemaType.UINT256;
    _valueSchema[4] = SchemaType.UINT256;

    return SchemaLib.encode(_valueSchema);
  }

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "smartObjectId";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](5);
    fieldNames[0] = "fuelUnitVolume";
    fieldNames[1] = "fuelConsumptionPerMinute";
    fieldNames[2] = "fuelMaxCapacity";
    fieldNames[3] = "fuelAmount";
    fieldNames[4] = "lastUpdatedAt";
  }

  /**
   * @notice Register the table with its config.
   */
  function register(ResourceId _tableId) internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, getKeySchema(), getValueSchema(), getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register(ResourceId _tableId) internal {
    StoreCore.registerTable(_tableId, _fieldLayout, getKeySchema(), getValueSchema(), getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get fuelUnitVolume.
   */
  function getFuelUnitVolume(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelUnitVolume) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get fuelUnitVolume.
   */
  function _getFuelUnitVolume(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelUnitVolume) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set fuelUnitVolume.
   */
  function setFuelUnitVolume(ResourceId _tableId, uint256 smartObjectId, uint256 fuelUnitVolume) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((fuelUnitVolume)), _fieldLayout);
  }

  /**
   * @notice Set fuelUnitVolume.
   */
  function _setFuelUnitVolume(ResourceId _tableId, uint256 smartObjectId, uint256 fuelUnitVolume) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((fuelUnitVolume)), _fieldLayout);
  }

  /**
   * @notice Get fuelConsumptionPerMinute.
   */
  function getFuelConsumptionPerMinute(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelConsumptionPerMinute) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get fuelConsumptionPerMinute.
   */
  function _getFuelConsumptionPerMinute(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelConsumptionPerMinute) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set fuelConsumptionPerMinute.
   */
  function setFuelConsumptionPerMinute(
    ResourceId _tableId,
    uint256 smartObjectId,
    uint256 fuelConsumptionPerMinute
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((fuelConsumptionPerMinute)), _fieldLayout);
  }

  /**
   * @notice Set fuelConsumptionPerMinute.
   */
  function _setFuelConsumptionPerMinute(
    ResourceId _tableId,
    uint256 smartObjectId,
    uint256 fuelConsumptionPerMinute
  ) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((fuelConsumptionPerMinute)), _fieldLayout);
  }

  /**
   * @notice Get fuelMaxCapacity.
   */
  function getFuelMaxCapacity(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelMaxCapacity) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get fuelMaxCapacity.
   */
  function _getFuelMaxCapacity(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (uint256 fuelMaxCapacity) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set fuelMaxCapacity.
   */
  function setFuelMaxCapacity(ResourceId _tableId, uint256 smartObjectId, uint256 fuelMaxCapacity) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((fuelMaxCapacity)), _fieldLayout);
  }

  /**
   * @notice Set fuelMaxCapacity.
   */
  function _setFuelMaxCapacity(ResourceId _tableId, uint256 smartObjectId, uint256 fuelMaxCapacity) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((fuelMaxCapacity)), _fieldLayout);
  }

  /**
   * @notice Get fuelAmount.
   */
  function getFuelAmount(ResourceId _tableId, uint256 smartObjectId) internal view returns (uint256 fuelAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get fuelAmount.
   */
  function _getFuelAmount(ResourceId _tableId, uint256 smartObjectId) internal view returns (uint256 fuelAmount) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set fuelAmount.
   */
  function setFuelAmount(ResourceId _tableId, uint256 smartObjectId, uint256 fuelAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((fuelAmount)), _fieldLayout);
  }

  /**
   * @notice Set fuelAmount.
   */
  function _setFuelAmount(ResourceId _tableId, uint256 smartObjectId, uint256 fuelAmount) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((fuelAmount)), _fieldLayout);
  }

  /**
   * @notice Get lastUpdatedAt.
   */
  function getLastUpdatedAt(ResourceId _tableId, uint256 smartObjectId) internal view returns (uint256 lastUpdatedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get lastUpdatedAt.
   */
  function _getLastUpdatedAt(ResourceId _tableId, uint256 smartObjectId) internal view returns (uint256 lastUpdatedAt) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set lastUpdatedAt.
   */
  function setLastUpdatedAt(ResourceId _tableId, uint256 smartObjectId, uint256 lastUpdatedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((lastUpdatedAt)), _fieldLayout);
  }

  /**
   * @notice Set lastUpdatedAt.
   */
  function _setLastUpdatedAt(ResourceId _tableId, uint256 smartObjectId, uint256 lastUpdatedAt) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((lastUpdatedAt)), _fieldLayout);
  }

  /**
   * @notice Get the full data.
   */
  function get(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (DeployableFuelBalanceData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    (bytes memory _staticData, PackedCounter _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(
    ResourceId _tableId,
    uint256 smartObjectId
  ) internal view returns (DeployableFuelBalanceData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    (bytes memory _staticData, PackedCounter _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    ResourceId _tableId,
    uint256 smartObjectId,
    uint256 fuelUnitVolume,
    uint256 fuelConsumptionPerMinute,
    uint256 fuelMaxCapacity,
    uint256 fuelAmount,
    uint256 lastUpdatedAt
  ) internal {
    bytes memory _staticData = encodeStatic(
      fuelUnitVolume,
      fuelConsumptionPerMinute,
      fuelMaxCapacity,
      fuelAmount,
      lastUpdatedAt
    );

    PackedCounter _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    ResourceId _tableId,
    uint256 smartObjectId,
    uint256 fuelUnitVolume,
    uint256 fuelConsumptionPerMinute,
    uint256 fuelMaxCapacity,
    uint256 fuelAmount,
    uint256 lastUpdatedAt
  ) internal {
    bytes memory _staticData = encodeStatic(
      fuelUnitVolume,
      fuelConsumptionPerMinute,
      fuelMaxCapacity,
      fuelAmount,
      lastUpdatedAt
    );

    PackedCounter _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(ResourceId _tableId, uint256 smartObjectId, DeployableFuelBalanceData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.fuelUnitVolume,
      _table.fuelConsumptionPerMinute,
      _table.fuelMaxCapacity,
      _table.fuelAmount,
      _table.lastUpdatedAt
    );

    PackedCounter _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(ResourceId _tableId, uint256 smartObjectId, DeployableFuelBalanceData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.fuelUnitVolume,
      _table.fuelConsumptionPerMinute,
      _table.fuelMaxCapacity,
      _table.fuelAmount,
      _table.lastUpdatedAt
    );

    PackedCounter _encodedLengths;
    bytes memory _dynamicData;

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  )
    internal
    pure
    returns (
      uint256 fuelUnitVolume,
      uint256 fuelConsumptionPerMinute,
      uint256 fuelMaxCapacity,
      uint256 fuelAmount,
      uint256 lastUpdatedAt
    )
  {
    fuelUnitVolume = (uint256(Bytes.slice32(_blob, 0)));

    fuelConsumptionPerMinute = (uint256(Bytes.slice32(_blob, 32)));

    fuelMaxCapacity = (uint256(Bytes.slice32(_blob, 64)));

    fuelAmount = (uint256(Bytes.slice32(_blob, 96)));

    lastUpdatedAt = (uint256(Bytes.slice32(_blob, 128)));
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   *
   *
   */
  function decode(
    bytes memory _staticData,
    PackedCounter,
    bytes memory
  ) internal pure returns (DeployableFuelBalanceData memory _table) {
    (
      _table.fuelUnitVolume,
      _table.fuelConsumptionPerMinute,
      _table.fuelMaxCapacity,
      _table.fuelAmount,
      _table.lastUpdatedAt
    ) = decodeStatic(_staticData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(ResourceId _tableId, uint256 smartObjectId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(ResourceId _tableId, uint256 smartObjectId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    uint256 fuelUnitVolume,
    uint256 fuelConsumptionPerMinute,
    uint256 fuelMaxCapacity,
    uint256 fuelAmount,
    uint256 lastUpdatedAt
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(fuelUnitVolume, fuelConsumptionPerMinute, fuelMaxCapacity, fuelAmount, lastUpdatedAt);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    uint256 fuelUnitVolume,
    uint256 fuelConsumptionPerMinute,
    uint256 fuelMaxCapacity,
    uint256 fuelAmount,
    uint256 lastUpdatedAt
  ) internal pure returns (bytes memory, PackedCounter, bytes memory) {
    bytes memory _staticData = encodeStatic(
      fuelUnitVolume,
      fuelConsumptionPerMinute,
      fuelMaxCapacity,
      fuelAmount,
      lastUpdatedAt
    );

    PackedCounter _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(uint256 smartObjectId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = bytes32(uint256(smartObjectId));

    return _keyTuple;
  }
}
